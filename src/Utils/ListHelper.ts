export class ListHelper {
  /**
   * Get all commands signature and description from command.
   *
   * @param command
   */
  static async getAllCommandsFrom(command: string) {
    // const commands: any = {}

    // await Artisan.call(`${command} --help`)

    return command
  }
}
